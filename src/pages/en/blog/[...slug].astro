---
import SlugBlogPage from "@pages/blog/[...slug].astro";
import { getCollection } from "astro:content";

export async function getStaticPaths() {
  return (await getCollection("blog")).map(({ slug }) => ({
    params: { slug },
  }));
}
---

<SlugBlogPage />
